#ifndef HOLBERTON_H
#define HOLBERTON_H

/* _putchar function prototype */
int _putchar(char);

/* 0. reset_to_98 function prototype */
void reset_to_98(int *n);

/* 1. */
void swap_int(int, int);

/* 2. */
int _strlen(char);

/* 3. */
void _puts(char);

/* 4. */
void print_rev(char);

/* 5. */
void rev_string(char);

/* 6. */
void puts2(char);

/* 7. */
void puts_half(char);

/* 8. */
void print_array(int, int);

/* 9. *_strcpy function prototype */
char *_strcpy(char, char);
#endif
