#include <stdio.h>
/**
* main - Print size of written value
* Return: return function 0 value at end
*/
int main(void)
{
char character;
int integer;
long int longInteger;
long long int longlongInteger;
float decimal;

printf(sizeof(character));
printf(sizeof(integer));
printf(sizeof(longInteger));
ptintf(sizeof(longlongInteger));
printf(sizeof(decimal));

return (0);
}
